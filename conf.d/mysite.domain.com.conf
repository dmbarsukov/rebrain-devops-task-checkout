 server {
  listen   80;

  root /var/www/mysite.domain.com/public;
  index index.html index.htm;

  server_name mysite.domain.com;
  
  location / {
   default_type "text/html";
   try_files $uri.html $uri $uri/ /index.html;
  }
}
